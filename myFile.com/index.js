const addDate = require(date - fns / addDate)
const dateX = date => {
  const result = addDate(new Date(2020, 7, 22), date)

  return `${result.getDate()}-${result.getmonth()}-${result.getYear()}`
}
console.log(dateX)
